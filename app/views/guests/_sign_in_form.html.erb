<%= form_for("user", url: user_session_path, html: { class: "well well-elderflower sign-in no-actions" }) do |f| %>
  <%= content_tag :fieldset do %>
    <legend>Есть аккаунт в системе?</legend>
    <div class="form-inline form-field">
      <%= f.email_field :email, placeholder: "Ваш Email", class: 'input-medium' %>
      <%= f.password_field :password, placeholder: "Пароль", class: 'input-medium' %>
      <%= f.submit "Войти", class: 'btn btn-success btn-medium' %>
    </div>
    <div class='form-inline form-helper'>
      <%= f.label :remember_me, class: 'checkbox' do %>
        <%= f.check_box :remember_me %> Запомните меня
      <% end %>
      <%= link_to "Забыли пароль?", new_password_path('user'), class: 'pull-right' %>
    </div>
  <% end %>
<% end %>